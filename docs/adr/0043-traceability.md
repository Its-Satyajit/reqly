# ADR 0043: Traceability Map — Roadmap to Implementation (M76)

## Status
Accepted — grill Q1 (docs)

## Context
ROADMAP P0-P5 + Milestones/01-06 drifted; need single source of truth and traceability.

## Decision
`Milestones/12-traceability-map.md` (M76): table `Milestone → ROADMAP § → core → CLI → desktop → tests → ADR/Spec` for M65-M75 (workflow→collab server) + `Milestones/06` Progress Tracker updates (P4 100%, Quality ~95%) + docs consolidation (retire duplicates, re-seed, definition of done).

## Consequences
Q1: Manual — not generated, must be re-run after each milestone.
Q2: Subordinate docs remain — gui-roadmap and UI spec are lower precedence.
